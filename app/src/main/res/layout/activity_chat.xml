<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
                xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/grey"
                android:paddingBottom="@dimen/small_padding">

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@android:id/list"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:clickable="false"
        android:divider="@null"
        android:focusable="false"
        android:layout_above="@+id/chat_msgsend"/>

    <EditText
        android:id="@+id/chat_msgedit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_toLeftOf="@+id/chat_msgsend"
        android:layout_toStartOf="@+id/chat_msgsend"
        android:layout_alignTop="@+id/chat_msgsend"
        android:background="@android:color/white"
        android:hint="@string/edit_input"
        android:textCursorDrawable="@null"/>
    <Button
        android:id="@+id/chat_msgsend"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:background="@color/grey_dark"
        android:text="@string/send"
        android:textColor="@android:color/white"/>

</RelativeLayout>

